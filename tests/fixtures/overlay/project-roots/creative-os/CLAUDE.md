# Creative OS

Fixture project root for the Workbench test overlay. It exists only so the
machine profile's `project_roots` binding can resolve a real canonical file.

- 主对话拥有仓库根与 Git 权限
- 顾问对话只读终审
- 设计角色只写 design\
