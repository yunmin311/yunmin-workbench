你现在是 E 机上 Yunmin Workbench 的唯一执行者，不是顾问、评审、产品经理或架构讨论对话。你的职责是直接读取仓库与随附文档，执行当前明确任务，自己检查、修改、运行、验证；不要给我方案列表让我选择，不要重新定义产品，不要继续 donor research，也不要要求我代跑命令或转述给别的 Agent。

先读取随附文件，权威顺序固定：
1. E 上当前真实 Git / 文件 / 测试 / Harness Runtime；
2. Yunmin-Workbench-E-Long-Term-Baseline-FROZEN-v2-2026-08-27（当前执行基准）；
3. Yunmin-Workbench-REUSE-MAP-FINAL-v2-2026-08-27（工程复用 / donor / license 正本）；
4. Yunmin-Workbench-项目完整描述（历史产品合同，只用于理解产品为什么存在；其中已经被 Session Spine、后续实现和当前基准覆盖的旧“下一步/测试数/UI 形态”不得反向覆盖当前事实）。

D 已彻底退役，不存在任何可依赖的 D 盘、旧 worktree、旧 node_modules、旧对话或 dirty state。不要尝试恢复 D。当前已知接班锚点 main=e7bb5d5fcf6108081512af9249708dde595faed3，但你必须 fetch 后以 origin/main 实际值为准。

本轮唯一目标：完成 E CLEAN-ROOM GREEN。禁止顺手做 History/Search、Attention、UI redesign、Task Board、Runtime 扩张或新 donor 集成。

直接执行：
- 核验 clone / origin / branch / git status / package manager / Node；
- 修 pnpm-workspace.yaml allowBuilds，使全新 clone 可正常安装 electron/esbuild 等必要 build scripts；
- 在 package.json 固定 packageManager，并固定实际支持的 Node 版本（engines 或 .nvmrc，采用与仓库当前真实可用环境一致的最小改动）；
- 清除 tests/e2e/benchmarks 对 D:\ai-governance-system、D:\project 等旧机器绝对路径的依赖：Overlay 优先正式 env seam（如 GOV_OVERLAY），Project 路径走 machine profile/projectRoots 或明确 fixture；没有真实 Overlay 时必须明确 fixture/skip 原因，不能用“全部 skip”冒充通过；
- 更新过期 README 到当前真实状态：Session Spine + on-demand drawers；Codex Dispatch/Observe/Receipt 已实现；external session Resume 未实现；当前执行顺序 CLEAN-ROOM GREEN → History/Search → Attention；
- THIRD_PARTY_NOTICES.md 只把 “prototype branch” 等过期措辞改成正式 main/project 表述，不改变已核验的 Reasonix/Cockpit/cdesktop provenance/license；
- 不碰产品语义、Projection Integrity、Context/Packet contracts、R0 reliability，不为了 green 删除测试、降低断言或加无依据 retry。

验收必须从新的临时目录 clone origin/main，确保没有旧 node_modules/旧机器状态，然后依次运行：
pnpm install --frozen-lockfile → pnpm typecheck → pnpm build → pnpm test → npx playwright test。
同时确认 frozen corruption、missing Codex、single instance、Overlay no-write 等关键 reliability coverage 仍存在，并报告真实 pass/skip 与原因。

若全部 GREEN：只提交本轮必要的 reproducibility / path portability / README / notices 修改，commit + push；重新 fetch，确认 local HEAD == origin/main；最后报告最终 SHA、clean-room clone 路径、install/typecheck/build/Vitest/Playwright 结果、skip、git status，并明确写：
CLEAN-ROOM GREEN — E LONG-TERM BASELINE ACTIVE

若失败：继续定位和修复；如果确实存在外部阻塞，只返回精确 blocker + 已验证证据，不要宣称 ACTIVE。